#include <stdio.h>
#include "main.h"
/**
 *main - prints the program name
 *@argc: an int
 *@argv: a string array
 *Return: 0 always
 */
int main(int argc, char *argv[])
{
printf("%s", argv[0]);
return (0);
}
