#include "main.h"
/**
 *reverse_array - Reverses an array
 *@a: an array
 *@n: length of the array
 */
void reverse_array(int *a, int n)
{
int i = 0, j = n - 1, pivot;
while (i < k)
{
pivot = a[i];
a[i] = a[j];
a[j] = pivot;
i++;
j--;
}
}